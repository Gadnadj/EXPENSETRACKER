import React from 'react';

type Props = {
    activeMenu: string;
}

const SideMenu = ({ activeMenu }: Props) => {
    return (
        <div>
            SideMenu
        </div>
    );
};

export default SideMenu;